#======================== override
alias ls="ls -hv --color=auto"
alias cmatrix="cmatrix -C blue"
alias diff="diff --color=auto"
alias grep="grep --color=auto"
alias msfconsole="msfconsole --quiet -x \"db_connect ${USER}@msf\""

#======================== shortcuts
# [ ! -z $DISPLAY ] && alias ls="exa"
alias wi="which"
alias s="sudo"
alias si="sudo !!"
alias su="sudo -i"
alias la="ls -la"
alias sctl="sudo systemctl"
alias ka="killall"
alias rmf="rm -rf"
# alias jl="prime-run jupyter lab"
# alias jn="prime-run jupyter-notebook"
# alias mm="micromamba"
# alias mma="micromamba activate"
# alias mmd="micromamba deactivate"
# alias b="distrobox"
# alias bs="~/.sh/util_distroboxsetup"
# alias br="~/.sh/util_browse"
# alias fm="~/.sh/util_filemanager"
alias nb="newsboat"
alias hm='cd ~'
alias nev="nix-env"
alias nch="nix-channel"

#==== git
alias g="git"
alias gs="git status"
alias gl="git log --graph --pretty=format:'%C(auto) %h | %s | %an | %ar%d'"
alias gcm="git commit"
alias gcma="git commit --amend --no-verify --no-edit"
alias gacm="git commit -a"
alias ga="git add"
alias gb="git branch"
alias gco="git checkout"
alias gr="git restore"
alias gpl="git pull"
alias gps="git push"
alias gpsf="git push --no-verify"
alias gf="git fetch"
alias gd="git diff"
alias gst="git stash"
alias gm="git merge"
alias grb="git rebase"
alias grs="git reset"
alias gwt="git worktree"
alias gcl="git clone"
alias gclean="git clean -f"

#==== tmux/inator
alias t="tmux"
alias tk="tmux kill-session"
alias tl="tmux list-session"
alias ta="tmux attach"
alias td="tmux detach"
alias tm="tmuxinator"
alias tme="tmuxinator edit"
alias tms="tmuxinator start"
alias tml="tmuxinator list"

#==== todoist
alias ti="todoist"

#==== yarn/bun/pnpm/...
alias p="pnpm"
alias pi="pnpm install"
alias pa="pnpm add"
alias pr="pnpm remove"
alias pd="pnpm run dev"
alias pl="pnpm run lint"
alias ps="pnpm run start"
alias pt="pnpm run test"
alias pc="pnpm run check"
alias pb="pnpm run build"
alias ppp="pnpm run prepush"

#==== pulumi
alias tf="pulumi"

#======================== utils
alias myip="curl ifconfig.me"
alias lsusr="awk -F: '{ print $1 }' /etc/passwd"
alias lsz="du -hd 1 | sort -h"
alias pastex="pngpaste"
alias mimedef="mimeopen --ask-default"
alias btrfs-tree="sudo btrfs subvol list / | cut -f9 -d' ' | sed -e 's/^/top\//' | as-tree"

#======================== safe
alias rm="rm -I"
alias mv="mv -i"
alias cp="cp -ri"
alias mkdir="mkdir -p"
alias ln="ln -i"
alias nt="cd ~/notes && nvim ."
